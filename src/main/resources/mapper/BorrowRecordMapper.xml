<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xs.booksystem.mapper.BorrowRecordMapper">

    <resultMap id="BaseResultMap" type="com.xs.booksystem.pojo.DTO.BorrowRecordDTO">
            <id property="id" column="id" jdbcType="INTEGER"/>
            <result property="userId" column="user_id" jdbcType="INTEGER"/>
            <result property="bookId" column="book_id" jdbcType="INTEGER"/>
            <result property="borrowTime" column="borrow_time" jdbcType="TIMESTAMP"/>
            <result property="dueTime" column="due_time" jdbcType="TIMESTAMP"/>
            <result property="returnTime" column="return_time" jdbcType="TIMESTAMP"/>
            <result property="status" column="status" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,book_id,
        borrow_time,due_time,return_time,
        status,create_time,update_time
    </sql>
    <select id="selectBorrowRecordWithDetails" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        br.id,
        br.user_id,
        br.book_id,
        br.borrow_time,
        br.due_time,
        br.return_time,
        br.status,
        br.create_time,
        br.update_time,
        b.title AS book_name,
        u.username
        FROM borrow_record br
        LEFT JOIN book b ON br.book_id = b.id
        LEFT JOIN user u ON br.user_id = u.id
        WHERE 1=1
        <if test="userId != null">
            AND br.user_id = #{userId}
        </if>
    </select>
</mapper>
